# 成品文件

运行 `npm run generate` 后再运行 `npm run export`，即可在 `成品文件/github-pages/` 得到完整静态站。

GitHub Pages 的自动部署直接使用 `.output/public`，本目录用于本地交付与离线检查。

当前站点版本：2026.09。搜索与分类状态可恢复；百科每页 24 条；主线清单 9 组、31 节点；Boss 目录 15 项。

在项目根目录运行 `npm.cmd run preview:static`，访问 `http://127.0.0.1:4173/jihuang/`。不要直接双击 HTML；页面资源使用 `/jihuang/` 子路径。

本轮已完成本地生成、浏览器回归与哈希验证，尚未发布线上。[交付报告](../过程文件/验证报告/2026-09-09-完善交付报告.md)包含截图、验证证据与范围说明。

