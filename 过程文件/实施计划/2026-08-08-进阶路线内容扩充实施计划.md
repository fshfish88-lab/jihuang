# 火堆边百科进阶路线内容扩充 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 本项目按用户“直接开始制作”的要求在当前会话内执行，不分派子代理。

**Goal:** 在现有 168 条结构化百科基础上新增 90 条进阶百科和 6 篇路线攻略，完成四季、遗迹、航海、Boss、天体裂隙与暗影圣所内容，并发布到 GitHub Pages。

**Architecture:** 新百科按六条路线拆分为六个数据文件，通过现有 `wikiEntries` 合并、搜索和静态路由体系发布。为生物与 Boss 增加可选的结构化战斗字段，为所有进阶条目增加路线归属与总攻略链接；六篇路线长文继续放入 Nuxt Content，数值只在百科数据维护。

**Tech Stack:** Nuxt 3、Vue 3、TypeScript、Nuxt Content、Vitest、Node.js、GitHub Actions、GitHub Pages

---

## 文件结构

### 新建文件

- `app/data/wiki/seasonal.ts`：四季生存 15 条百科。
- `app/data/wiki/caves-advanced.ts`：洞穴遗迹 15 条百科。
- `app/data/wiki/ocean-lunar.ts`：航海月岛 15 条百科。
- `app/data/wiki/boss-combat.ts`：Boss 战备 15 条百科。
- `app/data/wiki/lunar-rift.ts`：天体与月亮裂隙 15 条百科。
- `app/data/wiki/shadow-sanctum.ts`：暗影裂隙与圣所 15 条百科。
- `app/components/wiki/CombatGuidePanel.vue`：生物与 Boss 的准备、步骤、撤退与掉落面板。
- `content/progression/seasonal-cycle.md`：四季循环进阶路线。
- `content/progression/ruins-expedition.md`：遗迹远征进阶路线。
- `content/progression/ocean-lunar-expedition.md`：航海与月岛远征。
- `content/progression/boss-logistics.md`：Boss 战备总表。
- `content/progression/celestial-lunar-rift.md`：天体与月亮裂隙路线。
- `content/progression/shadow-sanctum.md`：暗影裂隙与圣所路线。
- `tests/unit/advanced-routes.test.ts`：六条路线的数量、字段、战斗数据和关联验证。

### 修改文件

- `app/types/wiki.ts`：增加路线、战斗和掉落类型。
- `app/data/wiki/shared.ts`：支持路线、路线攻略、额外官方来源和战斗数据。
- `app/data/wiki/index.ts`：合并六个路线包并导出路线统计。
- `app/components/wiki/WikiDetail.vue`：显示战斗面板和路线攻略入口。
- `app/assets/styles/components.css`：战斗面板及路线入口样式。
- `app/data/content.ts`：注册 6 篇进阶路线攻略。
- `tests/unit/wiki-data.test.ts`：总量提升到 258，保持原料理规则。
- `tests/unit/content.test.ts`：世界主线由 3 篇增至 9 篇。
- `tests/unit/search.test.ts`：新增路线名、Boss 俗称和 2026 新物品搜索。
- `scripts/sync-wiki-icons.mjs`：增加缺少镜像的图标来源覆盖并更新核对日期。
- `scripts/validate-wiki-data.mjs`：提高数量门槛并校验图片清单与路线攻略文件完整性；路线、战斗和来源语义由 Vitest 直接读取 TypeScript 数据校验。
- `素材文件/物品图标/来源清单.json`：增加 90 张图片记录。
- `public/images/wiki/*.png`：增加 90 张对应图标。
- `过程文件/验证报告/2026-08-08-进阶路线扩充验证报告.md`：记录最终验证证据。
- `成品文件/github-pages/**`：重新导出的静态成品。

## 90 条固定清单

每组必须恰好 15 条；标题可在核对游戏内简体中文后微调，但 slug 不得在实施中随意改变。

### 四季生存

| slug | 中文名 | 类型 |
|---|---|---|
| `winter-hat` | 冬帽 | 装备 |
| `puffy-vest` | 厚实背心 | 装备 |
| `rain-coat` | 雨衣 | 装备 |
| `eyebrella` | 眼球伞 | 装备 |
| `chilled-amulet` | 寒冰护符 | 装备 |
| `luxury-fan` | 羽毛扇 | 工具 |
| `endothermic-fire` | 吸热营火 | 建筑 |
| `endothermic-fire-pit` | 吸热火坑 | 建筑 |
| `thermal-measurer` | 温度测量仪 | 建筑 |
| `desert-goggles` | 沙漠护目镜 | 装备 |
| `deerclops` | 独眼巨鹿 | 生物 |
| `moose-goose` | 麋鹿鹅 | 生物 |
| `dragonfly` | 龙蝇 | 生物 |
| `bearger` | 熊獾 | 生物 |
| `antlion` | 蚁狮 | 生物 |

### 洞穴遗迹

| slug | 中文名 | 类型 |
|---|---|---|
| `cave-entrance` | 洞穴入口 | 季节与探索 |
| `moggles` | 鼹鼠帽 | 装备 |
| `mushroom-planter` | 蘑菇农场 | 建筑 |
| `batilisk` | 蝙蝠 | 生物 |
| `bunnyman` | 兔人 | 生物 |
| `depth-worm` | 洞穴蠕虫 | 生物 |
| `slurper` | 啜食者 | 生物 |
| `splumonkey` | 穴居猴 | 生物 |
| `ruins` | 远古遗迹 | 季节与探索 |
| `ancient-pseudoscience-station` | 远古伪科学站 | 建筑 |
| `thulecite-crown` | 铥矿皇冠 | 装备 |
| `thulecite-suit` | 铥矿甲 | 装备 |
| `star-callers-staff` | 唤星者魔杖 | 装备 |
| `deconstruction-staff` | 解构魔杖 | 装备 |
| `ancient-guardian` | 远古守护者 | 生物 |

### 航海月岛

| slug | 中文名 | 类型 |
|---|---|---|
| `boat-patch` | 船补丁 | 工具 |
| `mast` | 桅杆 | 建筑 |
| `steering-wheel` | 舵轮 | 建筑 |
| `anchor` | 锚 | 建筑 |
| `oar` | 船桨 | 工具 |
| `boat-lantern` | 船灯 | 建筑 |
| `salt-box` | 盐盒 | 建筑 |
| `salt-crystals` | 盐晶 | 资源 |
| `cookie-cutter` | 饼干切割机 | 生物 |
| `lunar-island` | 月岛 | 季节与探索 |
| `stone-fruit` | 石果 | 资源 |
| `kelp-stalk` | 海带茎 | 资源 |
| `moon-moth` | 月蛾 | 生物 |
| `glass-cutter` | 玻璃刀 | 装备 |
| `moon-dial` | 月晷 | 建筑 |

### Boss 战备

| slug | 中文名 | 类型 |
|---|---|---|
| `tentacle-spike` | 触手尖刺 | 装备 |
| `night-armour` | 暗夜甲 | 装备 |
| `marble-suit` | 大理石甲 | 装备 |
| `bee-mine` | 蜜蜂地雷 | 工具 |
| `tooth-trap` | 犬牙陷阱 | 工具 |
| `pan-flute` | 排箫 | 工具 |
| `weather-pain` | 天气风向标 | 装备 |
| `bee-queen` | 蜂后 | 生物 |
| `klaus` | 克劳斯 | 生物 |
| `toadstool` | 蟾蜍王 | 生物 |
| `crab-king` | 帝王蟹 | 生物 |
| `malbatross` | 邪天翁 | 生物 |
| `eye-of-terror` | 恐怖之眼 | 生物 |
| `twins-of-terror` | 双子魔眼 | 生物 |
| `ancient-fuelweaver` | 远古织影者 | 生物 |

### 天体与月亮裂隙

| slug | 中文名 | 类型 |
|---|---|---|
| `celestial-altar` | 天体祭坛 | 建筑 |
| `celestial-portal` | 天体传送门 | 建筑 |
| `moon-storm` | 月亮风暴 | 季节与探索 |
| `restrained-static` | 约束静电 | 资源 |
| `celestial-champion` | 天体英雄 | 生物 |
| `enlightened-crown` | 启迪之冠 | 装备 |
| `lunar-rift` | 月亮裂隙 | 季节与探索 |
| `grazer` | 啃食者 | 生物 |
| `deadly-brightshade` | 致命亮茄 | 生物 |
| `brightshade-husk` | 亮茄外壳 | 资源 |
| `brightshade-staff` | 亮茄魔杖 | 装备 |
| `brightshade-sword` | 亮茄剑 | 装备 |
| `brightshade-helm` | 亮茄头盔 | 装备 |
| `brightshade-armour` | 亮茄盔甲 | 装备 |
| `brightshade-bomb` | 亮茄炸弹 | 装备 |

### 暗影裂隙与圣所

| slug | 中文名 | 类型 |
|---|---|---|
| `shadow-rift` | 暗影裂隙 | 季节与探索 |
| `dreadstone-helm` | 绝望石头盔 | 装备 |
| `dreadstone-armour` | 绝望石盔甲 | 装备 |
| `void-cowl` | 虚空风帽 | 装备 |
| `void-robe` | 虚空长袍 | 装备 |
| `shadow-reaper` | 暗影收割者 | 装备 |
| `sanctum` | 远古圣所 | 季节与探索 |
| `waymark-compass` | 路标罗盘 | 工具 |
| `ancient-guard-tower` | 远古守卫塔 | 生物 |
| `keystone` | 楔石 | 资源 |
| `geothermite` | 地热螨 | 生物 |
| `heat-gland` | 热腺体 | 资源 |
| `ardent-axe` | 炽热斧 | 工具 |
| `pyretic-pickaxe` | 炎热镐 | 工具 |
| `thermal-balm` | 热疗药膏 | 工具 |

---

### Task 1: 建立独立工作区并复测基线

**Files:**
- Read: `package.json`
- Read: `tests/unit/*.test.ts`
- Read: `过程文件/设计规范/2026-08-08-进阶路线内容扩充设计规范.md`

- [ ] **Step 1: 创建功能 worktree**

Run:

```powershell
git -c safe.directory='C:/Users/67588/Documents/饥荒百科全书' worktree add '.worktrees/advanced-routes' -b codex/advanced-routes
```

Expected: 创建 `C:/Users/67588/Documents/饥荒百科全书/.worktrees/advanced-routes` 并检出 `codex/advanced-routes`。

- [ ] **Step 2: 安装锁定依赖**

Run: `npm.cmd ci`

Expected: `nuxt prepare` 完成；不改动 `package-lock.json`。

- [ ] **Step 3: 运行基线验证**

Run:

```powershell
npm.cmd test
npm.cmd run validate:content
npm.cmd run typecheck
```

Expected: 19 项现有测试通过；168 条百科、168 张图标和 39 篇内容校验通过；类型检查退出码 0。

### Task 2: 扩展路线与战斗数据契约

**Files:**
- Modify: `app/types/wiki.ts`
- Modify: `app/data/wiki/shared.ts`
- Modify: `app/data/wiki/index.ts`
- Create: `tests/unit/advanced-routes.test.ts`

- [ ] **Step 1: 写入失败类型与辅助函数测试**

在 `tests/unit/advanced-routes.test.ts` 写入：

```ts
import { describe, expect, it } from 'vitest'
import { wikiEntries } from '../../app/data/wiki'
import type { CombatInfo, WikiRoute } from '../../app/types/wiki'

describe('advanced route contract', () => {
  it('accepts the six fixed routes and structured combat fields', () => {
    const route: WikiRoute = '四季生存'
    const combat: CombatInfo = {
      spawn: ['冬季世界计时达到对应阶段'],
      preparation: ['至少准备两套护甲'],
      steps: ['引离基地后再战斗'],
      retreat: ['护甲耗尽时撤退'],
      drops: [{ slug: 'deerclops-eyeball', name: '独眼巨鹿眼球', amount: '1' }]
    }
    expect(route).toBe('四季生存')
    expect(combat.drops[0].slug).toBe('deerclops-eyeball')
  })

  it('keeps the existing encyclopedia intact before route data is added', () => {
    expect(wikiEntries).toHaveLength(168)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm.cmd test -- tests/unit/advanced-routes.test.ts`

Expected: FAIL，提示 `CombatInfo` 或 `WikiRoute` 尚未导出。

- [ ] **Step 3: 增加完整类型**

在 `app/types/wiki.ts` 增加：

```ts
export type WikiRoute =
  | '四季生存'
  | '洞穴遗迹'
  | '航海月岛'
  | 'Boss战备'
  | '天体裂隙'
  | '暗影圣所'

export interface DropInfo {
  slug?: string
  name: string
  amount: string
  note?: string
}

export interface CombatInfo {
  spawn: string[]
  preparation: string[]
  steps: string[]
  retreat: string[]
  drops: DropInfo[]
}
```

并在 `WikiEntry` 增加：

```ts
route?: WikiRoute
routeGuide?: string
combat?: CombatInfo
```

- [ ] **Step 4: 扩展 `SimpleSeed`**

在 `app/data/wiki/shared.ts` 的 `SimpleSeed` 增加：

```ts
route?: WikiRoute
routeGuide?: string
combat?: CombatInfo
sources?: SourceLink[]
```

同时让 `EntrySeed` 明确接收 `sourceLinks?: SourceLink[]`。`simpleEntry()` 将 `route`、`routeGuide`、`combat` 原样传入，并把 `sources` 映射为 `sourceLinks`；`createEntry()` 在展开条目字段前移除 `sourceLinks`，再把它追加到默认 Wiki 来源之后，避免把内部辅助字段泄漏进 `WikiEntry`。把版本常量更新为：

```ts
export const DATA_VERSION = 'DST 2026.08'
export const VERIFIED_AT = '2026-08-08'
```

- [ ] **Step 5: 导出路线统计函数**

在 `app/data/wiki/index.ts` 增加：

```ts
export function getWikiEntriesByRoute(route: WikiRoute): WikiEntry[] {
  return wikiEntries.filter(entry => entry.route === route)
}
```

- [ ] **Step 6: 运行测试**

Run: `npm.cmd test -- tests/unit/advanced-routes.test.ts`

Expected: 2 项测试通过。

- [ ] **Step 7: 提交**

```powershell
git add app/types/wiki.ts app/data/wiki/shared.ts app/data/wiki/index.ts tests/unit/advanced-routes.test.ts
git commit -m "feat: define advanced route combat contract"
```

### Task 3: 构建战斗面板与路线入口

**Files:**
- Create: `app/components/wiki/CombatGuidePanel.vue`
- Modify: `app/components/wiki/WikiDetail.vue`
- Modify: `app/assets/styles/components.css`

- [ ] **Step 1: 创建战斗面板**

`CombatGuidePanel.vue` 接收 `CombatInfo`，依次显示“出现条件、准备清单、处理步骤、撤退信号、主要掉落”；掉落带 slug 时链接 `/wiki/<slug>`，无 slug 时显示纯文本。

核心模板：

```vue
<script setup lang="ts">
import type { CombatInfo } from '~/types/wiki'
defineProps<{ combat: CombatInfo }>()
</script>

<template>
  <section class="wiki-section combat-guide paper-card">
    <p class="eyebrow">Combat File</p>
    <h2>战斗与处理方法</h2>
    <div class="combat-guide__grid">
      <div><h3>出现条件</h3><ol><li v-for="item in combat.spawn" :key="item">{{ item }}</li></ol></div>
      <div><h3>准备清单</h3><ul><li v-for="item in combat.preparation" :key="item">{{ item }}</li></ul></div>
      <div><h3>处理步骤</h3><ol><li v-for="item in combat.steps" :key="item">{{ item }}</li></ol></div>
      <div><h3>撤退信号</h3><ul><li v-for="item in combat.retreat" :key="item">{{ item }}</li></ul></div>
    </div>
    <h3>主要掉落</h3>
    <ul class="combat-guide__drops">
      <li v-for="drop in combat.drops" :key="`${drop.name}-${drop.amount}`">
        <NuxtLink v-if="drop.slug" :to="`/wiki/${drop.slug}`">{{ drop.name }}</NuxtLink>
        <span v-else>{{ drop.name }}</span>
        <strong>{{ drop.amount }}</strong><small v-if="drop.note">{{ drop.note }}</small>
      </li>
    </ul>
  </section>
</template>
```

- [ ] **Step 2: 接入详情页**

在 `WikiDetail.vue` 的制作/获取模块之后加入：

```vue
<WikiCombatGuidePanel v-if="entry.combat" :combat="entry.combat" />
<NuxtLink
  v-if="entry.routeGuide"
  class="wiki-route-link paper-card"
  :to="`/progression/${entry.routeGuide}`"
>
  查看本条目所在的完整进阶路线 →
</NuxtLink>
```

- [ ] **Step 3: 添加响应式样式**

`.combat-guide__grid` 桌面端两列、宽度低于 720px 时一列；掉落列表允许换行；路线入口沿用纸张边框、暗红箭头和现有焦点样式，不新增视觉体系。

- [ ] **Step 4: 类型检查与生成抽查**

Run:

```powershell
npm.cmd run typecheck
npm.cmd run generate
```

Expected: 类型检查通过；仍生成现有 406 条静态路由。

- [ ] **Step 5: 提交**

```powershell
git add app/components/wiki/CombatGuidePanel.vue app/components/wiki/WikiDetail.vue app/assets/styles/components.css
git commit -m "feat: add combat and route panels"
```

### Task 4: 第一批——四季生存与洞穴遗迹

**Files:**
- Create: `app/data/wiki/seasonal.ts`
- Create: `app/data/wiki/caves-advanced.ts`
- Create: `content/progression/seasonal-cycle.md`
- Create: `content/progression/ruins-expedition.md`
- Modify: `app/data/wiki/index.ts`
- Modify: `app/data/content.ts`
- Modify: `tests/unit/advanced-routes.test.ts`
- Modify: `tests/unit/content.test.ts`

- [ ] **Step 1: 写入第一批失败测试**

在 `advanced-routes.test.ts` 增加固定 slug 数组，并断言：

```ts
it.each([
  ['四季生存', seasonalSlugs],
  ['洞穴遗迹', caveSlugs]
] as const)('%s route contains its exact 15 entries', (route, expected) => {
  const entries = wikiEntries.filter(entry => entry.route === route)
  expect(entries.map(entry => entry.slug).sort()).toEqual([...expected].sort())
  expect(entries.every(entry => entry.routeGuide)).toBe(true)
})
```

Boss/敌对生物额外断言 `combat` 的五个数组均非空；装备和建筑继续执行制作或获取规则。

- [ ] **Step 2: 运行测试确认失败**

Run: `npm.cmd test -- tests/unit/advanced-routes.test.ts`

Expected: FAIL，四季和洞穴路线均为 0 条。

- [ ] **Step 3: 录入四季 15 条**

`seasonal.ts` 严格使用本计划“四季生存”表的 15 个 slug。全部设置：

```ts
route: '四季生存'
routeGuide: 'seasonal-cycle'
tags: ['四季生存', '对应季节', '条目类型']
```

五个 Boss 写完整 `combat`；装备/建筑写材料、科技、关键数值、操作步骤、误区和关联掉落。独眼巨鹿、熊獾等虽已有 `/bosses` 长文，新的结构化档案仍使用 `/wiki/<slug>`，并在 `related` 指向掉落物。

- [ ] **Step 4: 录入洞穴 15 条**

`caves-advanced.ts` 严格使用本计划“洞穴遗迹”表的 15 个 slug。全部设置：

```ts
route: '洞穴遗迹'
routeGuide: 'ruins-expedition'
tags: ['洞穴遗迹', '洞穴或遗迹', '条目类型']
```

敌对生物与远古守护者写完整 `combat`；远古装备写制作材料、远古伪科学站完整/损坏状态要求、耐久与特殊效果。

- [ ] **Step 5: 写两篇路线攻略**

两份 Markdown 使用完整 frontmatter，`version: "2026.08"`、`updatedAt: "2026-08-08"`。正文必须依次包含：开始阶段、前置条件、最小准备、节点路线、战斗/环境处理、撤退信号、收益、相关百科、来源。

- [ ] **Step 6: 注册数据和攻略**

`app/data/wiki/index.ts` 合并 `seasonalEntries` 与 `advancedCaveEntries`；`app/data/content.ts` 增加 `seasonal-cycle`、`ruins-expedition` 两个 `progression` 元数据对象。

- [ ] **Step 7: 运行测试**

Run:

```powershell
npm.cmd test -- tests/unit/advanced-routes.test.ts tests/unit/content.test.ts
npm.cmd run typecheck
```

Expected: 百科总量 198；两条路线各 15；世界主线 5 篇；类型检查通过。

- [ ] **Step 8: 提交**

```powershell
git add app/data/wiki app/data/content.ts content/progression tests/unit
git commit -m "feat: add seasonal and ruins progression packs"
```

### Task 5: 第一批图标与来源闭环

**Files:**
- Modify: `scripts/sync-wiki-icons.mjs`
- Modify: `public/images/wiki/*.png`
- Modify: `素材文件/物品图标/来源清单.json`
- Modify: `scripts/validate-wiki-data.mjs`

- [ ] **Step 1: 更新图标同步日期与覆盖表**

把清单 `verifiedAt` 更新为 `2026-08-08`。对镜像不存在或名称不匹配的条目，在 `sourceOverrides` 中使用对应 Wiki 文件页的 PNG 直链，至少覆盖环境、Boss 和远古站图标。

- [ ] **Step 2: 同步 198 张图标**

Run: `npm.cmd run sync:icons`

Expected: `Downloaded 198/198 wiki icons.`；错误数 0。

- [ ] **Step 3: 提高验证门槛**

`validate-wiki-data.mjs` 将最低数量从 120 改为 198，并继续校验 PNG 签名、字节数、SHA-256、来源页和 Klei 署名。

- [ ] **Step 4: 抽查第一批图标**

打开 `eyebrella.png`、`dragonfly.png`、`antlion.png`、`moggles.png`、`ancient-pseudoscience-station.png`、`thulecite-crown.png`、`ancient-guardian.png`，确认不是占位、皮肤或错误生物。

- [ ] **Step 5: 运行校验并提交**

Run: `npm.cmd run validate:content`

Expected: 41 篇内容、198 条百科与 198 张图标，0 errors。

```powershell
git add scripts public/images/wiki 素材文件/物品图标
git commit -m "assets: add seasonal and ruins icons"
```

### Task 6: 第二批——航海月岛与 Boss 战备

**Files:**
- Create: `app/data/wiki/ocean-lunar.ts`
- Create: `app/data/wiki/boss-combat.ts`
- Create: `content/progression/ocean-lunar-expedition.md`
- Create: `content/progression/boss-logistics.md`
- Modify: `app/data/wiki/index.ts`
- Modify: `app/data/content.ts`
- Modify: `tests/unit/advanced-routes.test.ts`
- Modify: `tests/unit/content.test.ts`

- [ ] **Step 1: 写入第二批失败测试**

为“航海月岛”和“Boss战备”各加入固定 15 slug 断言；8 个 Boss 必须包含战斗准备、步骤、撤退信号和掉落。

- [ ] **Step 2: 运行确认失败**

Run: `npm.cmd test -- tests/unit/advanced-routes.test.ts`

Expected: 新增两组断言失败，现有两组仍通过。

- [ ] **Step 3: 录入航海月岛 15 条**

所有条目设置 `route: '航海月岛'`、`routeGuide: 'ocean-lunar-expedition'`。船体部件明确制作材料、放置方式和航行用途；月岛环境、生物和资源写定位、获取及后续科技关系。

- [ ] **Step 4: 录入 Boss 战备 15 条**

所有条目设置 `route: 'Boss战备'`、`routeGuide: 'boss-logistics'`。7 件装备/工具写关键数值和适用战斗；8 个 Boss 写完整结构化战斗字段及主要掉落。

- [ ] **Step 5: 写两篇路线攻略并注册**

航海攻略必须包含下水前清单、修船/照明/锚泊、月岛定位与返程；Boss 战备必须包含单人/多人耗材估算原则、场地布置、撤退阈值和收益优先级。注册两个 progression 元数据对象。

- [ ] **Step 6: 运行测试与提交**

Run:

```powershell
npm.cmd test -- tests/unit/advanced-routes.test.ts tests/unit/content.test.ts
npm.cmd run typecheck
```

Expected: 百科总量 228；四条路线各 15；世界主线 7 篇。

```powershell
git add app/data/wiki app/data/content.ts content/progression tests/unit
git commit -m "feat: add ocean and boss progression packs"
```

### Task 7: 第二批图标与来源闭环

**Files:**
- Modify: `scripts/sync-wiki-icons.mjs`
- Modify: `public/images/wiki/*.png`
- Modify: `素材文件/物品图标/来源清单.json`
- Modify: `scripts/validate-wiki-data.mjs`

- [ ] **Step 1: 为复杂条目补图标覆盖**

月岛环境、帝王蟹、双子魔眼、远古织影者等没有稳定镜像的条目使用对应 Wiki 文件直链；不得复用相似图标。

- [ ] **Step 2: 同步并验证 228 张图标**

Run:

```powershell
npm.cmd run sync:icons
npm.cmd run validate:content
```

Expected: 43 篇内容、228 条百科与 228 张图标，0 errors。

- [ ] **Step 3: 抽查并提交**

抽查船补丁、桅杆、盐盒、月岛、月蛾、帝王蟹、邪天翁、双子魔眼和远古织影者。

```powershell
git add scripts public/images/wiki 素材文件/物品图标
git commit -m "assets: add ocean and boss icons"
```

### Task 8: 第三批——天体裂隙与暗影圣所

**Files:**
- Create: `app/data/wiki/lunar-rift.ts`
- Create: `app/data/wiki/shadow-sanctum.ts`
- Create: `content/progression/celestial-lunar-rift.md`
- Create: `content/progression/shadow-sanctum.md`
- Modify: `app/data/wiki/index.ts`
- Modify: `app/data/content.ts`
- Modify: `tests/unit/advanced-routes.test.ts`
- Modify: `tests/unit/content.test.ts`

- [ ] **Step 1: 写入第三批失败测试**

为“天体裂隙”和“暗影圣所”各加入固定 15 slug 断言；所有 2026 新条目必须至少有一个 `kind: 'official'` 的来源。

- [ ] **Step 2: 运行确认失败**

Run: `npm.cmd test -- tests/unit/advanced-routes.test.ts`

Expected: 第三批两组失败，前四条路线仍通过。

- [ ] **Step 3: 录入天体与月亮裂隙 15 条**

全部设置 `route: '天体裂隙'`、`routeGuide: 'celestial-lunar-rift'`。亮茄装备写亮茄工坊、材料、位面伤害/防御、耐久和修复方式；裂隙与生物写不可逆开启条件、阶段、环境风险和资源循环。

- [ ] **Step 4: 录入暗影与圣所 15 条**

全部设置 `route: '暗影圣所'`、`routeGuide: 'shadow-sanctum'`。圣所条目写巨型漩涡入口、路标机制、房间风险、楔石用途与重置；2026 热腺体和热能工具引用 Klei `Cursed Confrontation Part 1` 更新及后续补丁。

- [ ] **Step 5: 写两篇路线攻略并注册**

天体攻略明确天体英雄、启迪碎片、裂隙开启和亮茄装备顺序；暗影攻略明确织影者前置、暗影裂隙、圣所导航、守卫塔战和撤离路线。注册两个 progression 元数据对象。

- [ ] **Step 6: 运行测试与提交**

Run:

```powershell
npm.cmd test -- tests/unit/advanced-routes.test.ts tests/unit/content.test.ts
npm.cmd run typecheck
```

Expected: 百科总量 258；六条路线各 15；世界主线 9 篇。

```powershell
git add app/data/wiki app/data/content.ts content/progression tests/unit
git commit -m "feat: add lunar rift and sanctum progression packs"
```

### Task 9: 第三批图标与官方来源闭环

**Files:**
- Modify: `scripts/sync-wiki-icons.mjs`
- Modify: `public/images/wiki/*.png`
- Modify: `素材文件/物品图标/来源清单.json`
- Modify: `scripts/validate-wiki-data.mjs`

- [ ] **Step 1: 补齐后期图标来源**

优先使用 Wiki 对应文件直链；天体祭坛、裂隙、啃食者、亮茄装备、圣所、远古守卫塔、热腺体和热能工具逐个添加 `sourceOverrides`。

- [ ] **Step 2: 同步 258 张图标**

Run: `npm.cmd run sync:icons`

Expected: `Downloaded 258/258 wiki icons.`。

- [ ] **Step 3: 完成最终数据验证器**

把最低数量提高为 258。该脚本继续拒绝图片清单缺项、错误 PNG、校验和不一致、来源页缺失，并通过六个固定攻略 slug 检查路线攻略文件是否存在。

以下语义规则由 `tests/unit/advanced-routes.test.ts` 直接导入 `wikiEntries` 校验，避免验证脚本只读取简化图标清单却误报成功：

- 路线条目缺 `routeGuide`；
- 任一路线不等于 15 条；
- 列入战斗清单的生物/Boss 缺少 `combat` 任一字段；
- 2026 新条目没有官方来源；
- 掉落中的 slug 既不在百科中、也不属于明确允许的未建档掉落。

- [ ] **Step 4: 抽查图标并提交**

至少抽查月亮裂隙、天体英雄、亮茄盔甲、亮茄炸弹、暗影裂隙、圣所、路标罗盘、远古守卫塔、热腺体、炽热斧和热疗药膏。

Run: `npm.cmd run validate:content`

Expected: 45 篇内容、258 条百科与 258 张图标，0 errors。

```powershell
git add scripts public/images/wiki 素材文件/物品图标
git commit -m "assets: add rift and sanctum icons"
```

### Task 10: 完成搜索、总量与回归测试

**Files:**
- Modify: `tests/unit/wiki-data.test.ts`
- Modify: `tests/unit/content.test.ts`
- Modify: `tests/unit/search.test.ts`
- Modify: `tests/unit/advanced-routes.test.ts`

- [ ] **Step 1: 锁定最终总量**

把百科总量测试更新为：

```ts
expect(wikiEntries).toHaveLength(258)
expect(wikiEntries.filter(item => item.category === '料理')).toHaveLength(30)
```

内容注册表测试更新为 9 篇 progression 和 258 篇 wiki。

- [ ] **Step 2: 增加搜索用例**

`search.test.ts` 增加：

```ts
it.each([
  ['眼球伞', 'eyebrella'],
  ['犀牛', 'ancient-guardian'],
  ['帝王蟹', 'crab-king'],
  ['亮茄炸弹', 'brightshade-bomb'],
  ['圣所', 'sanctum'],
  ['热腺体', 'heat-gland']
])('finds advanced query %s as %s', (query, slug) => {
  expect(index.search(query, { limit: 5 }).map(result => result.item.slug)).toContain(slug)
})
```

- [ ] **Step 3: 运行全部测试**

Run: `npm.cmd test`

Expected: 5 个测试文件全部通过，0 failures。

- [ ] **Step 4: 运行类型与内容校验**

Run:

```powershell
npm.cmd run validate:content
npm.cmd run typecheck
```

Expected: 45 篇内容、258 条百科、258 张图标、0 errors；类型检查退出码 0。

- [ ] **Step 5: 提交**

```powershell
git add tests scripts/validate-wiki-data.mjs
git commit -m "test: verify advanced route encyclopedia"
```

### Task 11: 静态生成与浏览器验收

**Files:**
- Create: `过程文件/验证报告/2026-08-08-进阶路线扩充验证报告.md`

- [ ] **Step 1: 完整静态生成**

Run: `npm.cmd run generate`

Expected: 成功生成原有路由、90 个新百科路由和 6 个新 progression 路由；无构建错误。

- [ ] **Step 2: 启动静态预览**

Run: `npm.cmd run preview:static`

Expected: 本地 `/jihuang/` 可访问。

- [ ] **Step 3: 桌面端浏览器抽查**

检查：

- `/wiki` 显示 258 条；
- `/progression` 显示 9 篇；
- `/wiki/deerclops` 显示战斗面板和四季路线入口；
- `/wiki/brightshade-bomb` 显示配方、位面伤害和天体路线入口；
- `/wiki/sanctum` 显示获取/进入步骤和暗影圣所路线入口；
- `/progression/shadow-sanctum` 内部链接可跳转。

- [ ] **Step 4: 手机端抽查**

使用约 390×844 视口检查战斗面板单列、掉落列表换行、路线入口可点击且无水平滚动。

- [ ] **Step 5: 写验证报告并提交**

报告记录测试数量、内容/图片数量、静态路由数量、抽查页面、控制台结果和已知非阻塞警告。

```powershell
git add 过程文件/验证报告/2026-08-08-进阶路线扩充验证报告.md
git commit -m "docs: record advanced route validation"
```

### Task 12: 导出成品、合并并部署

**Files:**
- Modify: `成品文件/github-pages/**`

- [ ] **Step 1: 导出静态成品**

Run: `npm.cmd run export`

Expected: `成品文件/github-pages/build-info.json` 指向当前源码提交，成品包含 258 张百科图标和全部新路由。

- [ ] **Step 2: 提交成品快照**

```powershell
git add -A -- 成品文件/github-pages
git diff --cached --check
git commit -m "build: export advanced progression site"
```

- [ ] **Step 3: 在功能分支最终复测**

Run:

```powershell
npm.cmd test
npm.cmd run validate:content
npm.cmd run typecheck
npm.cmd run generate
```

Expected: 全部退出码 0。

- [ ] **Step 4: 合并到 main**

在主工作区执行：

```powershell
git -c safe.directory='C:/Users/67588/Documents/饥荒百科全书' pull --ff-only origin main
git -c safe.directory='C:/Users/67588/Documents/饥荒百科全书' merge --ff-only codex/advanced-routes
```

Expected: 快进合并，无冲突。

- [ ] **Step 5: 在 main 再次复测**

Run:

```powershell
npm.cmd test
npm.cmd run validate:content
npm.cmd run typecheck
npm.cmd run generate
```

Expected: 全部退出码 0。

- [ ] **Step 6: 推送并等待 Pages**

```powershell
git -c safe.directory='C:/Users/67588/Documents/饥荒百科全书' push origin main
gh run list --repo fshfish88-lab/jihuang --branch main --limit 3
```

Expected: `Deploy Campfire Wiki` 对应提交状态为 `completed / success`。

- [ ] **Step 7: 线上复测**

确认正式站点 `/wiki/`、六篇路线、`/wiki/brightshade-bomb/`、`/wiki/sanctum/` 和代表性图片均为 HTTP 200；真实浏览器无新控制台错误。

- [ ] **Step 8: 清理工作区**

确认 main 已包含功能提交后，从主工作区删除 `.worktrees/advanced-routes` 并删除本地 `codex/advanced-routes`；不得删除 main 或远端成果。
