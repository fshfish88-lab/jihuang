# 制作道具与料理补全验证报告

- 验证日期：2026-08-13
- 分支：`codex/crafting-recipes`
- 最终构建提交：`ef28b29e5061a9be6d15c5db51f1233651eec349`
- 范围：制作道具、缺失材料及其配方依赖材料、料理、图标、站点统计、静态路由、GitHub Pages 成品与交付脚本安全性；网络部署状态在最终交付时另行核验。

## 一、最终规模

本轮补全覆盖 30 个指定可制作物、4 个原始缺失材料（发光浆果、注能月亮碎片、纯粹恐惧、暗影碎布）及为配方闭环补充的依赖材料，并新增 20 道实用四格料理。最终站点统计如下：

| 项目 | 数量 |
| --- | ---: |
| 制作物补全 | 30 |
| 新增实用料理 | 20 |
| 百科条目 | 328 |
| 其中料理 | 50 |
| 全站攻略条目 | 352 |
| 百科 PNG 图标 | 328 |

## 二、全量命令验证

下列命令均在本分支工作树根目录完整执行，退出码均为 0。

1. `npm.cmd run validate:content`
   - `Validated 45 content entries; 0 errors.`
   - `Validated 328 structured wiki entries and 328 attributed PNG icons; 0 errors.`
2. `npm.cmd test`
   - `Test Files 10 passed (10)`
   - `Tests 190 passed (190)`
3. `npm.cmd run typecheck`
   - `tsc --noEmit -p tsconfig.json`，退出码 0，无类型错误。
4. `npm.cmd run generate`
   - Nuxt 3.21.10 / Nitro 2.13.4 / Vite 7.3.6。
   - Nuxt Content 处理 6 个集合、45 个文件。
   - 客户端转换 371 个模块，服务端转换 283 个模块。
   - `Prerendered 738 routes`，成功生成 `.output/public`。

生成阶段有一条非致命警告：Nitro 在 Windows 工作树路径下无法解析主工作目录 `node_modules/@nuxt/nitro-server/dist/runtime/utils/cache-driver.js` 的 `file:///...` 绝对引用，因此按外部依赖处理。该警告未导致构建、预渲染或浏览器加载失败。

## 三、浏览器与视觉验证

使用项目 `scripts/preview-static.mjs` 机制对生成后的静态站点进行本地预览，并使用本机 Chrome + Playwright 自动化检查。Playwright 自带浏览器缓存当时缺失，故明确指定已安装的 Chrome；检查完成后预览进程已终止。

实际检查 16 个页面、2 种视口，共 32 次页面检查：

- 视口：桌面 `1440 × 1000`；移动端 `390 × 844`。
- 首页：`/`。
- 百科索引：`/wiki`。
- 材料：`/wiki/glow-berry`、`/wiki/infused-moon-shard`、`/wiki/pure-horror`、`/wiki/dark-tatters`。
- 制作物：`/wiki/compass`、`/wiki/blow-dart`、`/wiki/rabbit-earmuffs`、`/wiki/war-saddle`、`/wiki/glossamer-saddle`。
- 料理：`/wiki/fruit-medley`、`/wiki/lobster-dinner`、`/wiki/potato-souffle`、`/wiki/volt-goat-chaud-froid`。
- 工具：`/tools/progression-checklist`。

检查结果：

- 首页显示版本 `2026.08`，没有旧总数展示文案。
- `/wiki` 的页面文案、页面标题和 description SEO 均显示 328 份百科、50 道四格食谱。
- 所有样本页的中英文标题、阶段、配方材料、数量、科技站或烹饪规则均与结构化数据一致。
- 页面内全部图片完成加载且 `naturalWidth > 0`；`/wiki` 索引的 328 张图片也强制完成懒加载后逐张检查。
- 32 次页面检查共收集 360 个唯一站内链接，逐一请求均成功，没有断链。
- 两个视口的全部页面均满足 `document.documentElement.scrollWidth <= window.innerWidth`。
- 浏览器 console error 0，page error 0。
- 实际目检首页桌面长截图、战争牛鞍桌面长截图、水果圣代移动端长截图：配方卡、数量标记、侧栏、页脚与移动端单列布局可读，无明显遮挡或溢出。截图仅作为临时构建验证放在被忽略的 `.output/task7-qa`，未纳入 Git 成品。

## 四、高风险事实复核

- 羽毛映射：黑色羽毛用于催眠吹箭和鞍具脱卸器；红色羽毛用于火焰吹箭；蓝色羽毛用于攻击吹箭；黄色羽毛用于电击吹箭。测试额外保证蓝色羽毛不会误映射到电击吹箭。
- 指南针：金块 ×1、燧石 ×1，徒手制作。
- 兔耳罩：活兔子 ×2、树枝 ×1，徒手制作。
- 灌木丛帽：草帽 ×1、绳子 ×1、挖起的浆果丛 ×1，炼金引擎。
- 战争牛鞍：活兔子 ×4、钢羊毛 ×4、木头 ×10，炼金引擎。
- 水果圣代：水果度至少 3，不含肉度和蔬菜度；示例采用西瓜 ×3、树枝 ×1，避免冰与果酱的同优先级冲突。
- Live Eel：英文名使用 `Live Eel`，包含“活鳗鱼”别名，图标来源指向 `pondeel.png`，不再按错误文件名发现素材。

上述关键配方及元数据均受本轮 190 条单元测试中的专门契约断言保护。

## 五、图标、哈希与联系表

- 内容验证器按文件存在、PNG 签名、归属来源和受信哈希记录验证了 328/328 张百科图标。
- 浏览器对指定页面及百科索引图标做了实际解码验证，均满足 `naturalWidth > 0`。
- 已目检 `过程文件/验证报告/2026-08-13-制作道具料理图标抽查.png`：20 个高风险图标均与中文名和 slug 对应，包括 4 个缺失材料、四类吹箭、三类牛鞍、Live Eel、龙虾料理和水果圣代。
- 联系表 SHA-256：`17CE2AD46C63984C19C4B88DB35DBFBBBA01E0258987DA32C0DCB593571398AA`。

## 六、旧文案扫描

对显示层 `app/pages`、`app/components`、`app/layouts`、`app/data/site-metadata.ts` 扫描 `2026.07`、`168`、`39`、`首版路线`，排除 CSS 颜色数字后没有旧展示文案命中。导出的首页、百科页和关于页也没有这些旧文案。

`app/data/content.ts` 中仍有历史单条内容元数据 `version: '2026.07'`。它描述具体旧条目的内容版本，不是站点当前版本或统计展示，因此保留是正确的；站点级版本统一由 `SITE_VERSION = '2026.08'` 提供。

## 七、GitHub Pages 静态成品

执行 `npm.cmd run export`，退出码 0，更新 `成品文件/github-pages`：

- 成品共 1136 个文件，33,641,186 字节（32.08 MiB）。其中 1134 个站点文件受完整构建清单逐文件 SHA-256 保护，另含构建清单与 `build-info.json`。
- 构建清单绑定提交 `ef28b29e5061a9be6d15c5db51f1233651eec349`、站点子路径 `/jihuang/`，导出时已重新核对源目录、清单和 staging 三者的文件路径、大小与哈希。
- 已确认首页、百科索引、4 个材料页、5 个制作物页、4 个料理页、主线清单页及样本 PNG 均存在。
- 首页与样本详情 HTML 使用 `/jihuang/_nuxt/...`、`/jihuang/images/...` 等子路径；首页抽样到 37 个 `/jihuang/` 引用，指南针页抽样到 29 个。
- 导出 HTML 可检索到 `2026.08`、`328 份《饥荒联机版》核心档案`、`50 道四格食谱`、指南针材料与徒手制作、水果圣代配方规则。

## 八、交付安全复核

- 图标同步使用唯一 staging、带所有者和阶段的进程锁，以及提交前回滚、提交后清理的恢复协议；损坏图标只有在匹配来源清单 SHA-256 后才可替换。
- 已对路径穿越、活进程锁、死进程接管、备份损坏、清单提交前后崩溃和恢复失败保留救援副本做故障注入测试。
- 静态导出先验证生成完成清单，再复制到 staging；交换旧成品前后均核对旧目录身份，失败时恢复或保留 backup 与 journal，不会先删除最后一份成品。
- 已对源目录不完整、源与目标重叠、复制中途变化、同尺寸错误字节、进程崩溃、外部并发改写、提升与回滚同时失败做故障注入测试。
- 图标同步与静态导出分别通过独立规格审查和代码质量复审，未发现剩余 Critical 或 Important 问题。

## 九、结论

全量内容校验、190 条测试、TypeScript 检查、738 条路由静态生成、双视口浏览器检查和 GitHub Pages 成品核对均通过。当前本地静态成品已完成；线上 GitHub Pages 是否成功发布，以主分支推送后的 Actions 结果与公开 URL 实测为准。
