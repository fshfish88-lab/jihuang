# 火堆边百科扩充验证报告

验证日期：2026-07-31

## 数据与素材

- 结构化百科：168 条。
- 常用料理：30 道，每道示例食材总格数均为 4。
- 本地图标：168 张 PNG。
- 图片来源清单：168 条，均记录来源页、版权方、SHA-256 和文件字节数。
- 图物抽查：猪人、箱子、洋葱、发条骑士、肉丸、复活台等均对应正确。

## 自动化验证

- `npm run validate:content`：通过；39 篇原有长篇内容和 168 条结构化百科均无错误。
- `npm test`：通过；4 个测试文件、19 项测试全部通过。
- `npm run typecheck`：通过。
- `npm run generate`：通过；静态生成 406 个路由。
- `npm run export`：通过；成品含 197 个 HTML 文件和 168 张百科图标。

## 浏览器复测

- 桌面端肉丸页：图标、料理属性、食材链接、配方规则、禁止项、冲突提醒、获取步骤和来源均正常。
- 桌面端橄榄球头盔页：猪皮 ×1、绳子 ×1、炼金引擎、护甲分类、产出 1 个和获取步骤均正常。
- 手机端 390 × 844：页面无横向溢出，档案首屏可读。
- 百科索引：显示 168 份手记；点击“料理”后恰好显示 30 张卡片。
- 当前页面重新加载后控制台无 error 或 warning。

## 构建环境说明

独立 Git worktree 中 Nuxt 解析临时 tsconfig 时会向主工作区查找 `.nuxt/tsconfig.json`；本地验证前同步了该忽略目录中的生成文件。此文件不进入提交，GitHub Actions 在仓库根目录构建时不受影响。
