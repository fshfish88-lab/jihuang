# 火堆边百科 2026.09 完善交付报告

本轮按网站检查报告完善核心查阅体验，保留纸张、炭黑与暗红的荒野手记风格。已更新本地源码与静态成品，未提交 Git、未推送或发布线上网站。

## 已完成

| 项目 | 交付结果 |
| --- | --- |
| 搜索命中 | “怎么复活”等常见问句可匹配；准确名称与别名优先；展示完整匹配数量并可加载更多 |
| 返回与分享 | 搜索词、已加载批次、百科分类、路线、料理筛选及分页保存在 URL；返回与刷新恢复，浏览位置回归通过 |
| 百科索引 | 每页 24 条紧凑图文档案，共 14 页；完整覆盖 328 个条目 |
| 料理查找 | 新增回血 ≥20、回理智 ≥15、饱食 ≥75 和示例食材筛选；属性在列表直接展示 |
| 世界清单 | 由 3 组、13 节点扩展为 9 组、31 节点；新增四季、遗迹远征、航海进阶、战备、天体裂隙和暗影圣所；支持按路线查看、路线完成数和前置准备 |
| 旧进度兼容 | 保留原存储键及 13 个旧节点 ID，旧勾选继续生效；总节点增加后总完成百分比按新总数计算 |
| Boss 目录 | 将已有专题和百科战斗档案统一呈现，目录由 5 项增加到 15 项，支持地表、洞穴和海洋筛选 |
| 战备核对 | 百科战斗档案中的准备项可逐项勾选；这是本次阅读核对，离开后重新检查 |
| 阅读导航 | 攻略增加可折叠章节目录；百科增加配方、获取、战斗、用途、来源跳转与返回顶部 |
| 手机体验 | 压缩百科详情图标和顶部信息；手机搜索改为放大镜并提供明确标签；修正属性数字排版 |
| 小交互 | `/` 打开搜索且不会干扰输入框；Escape 关闭手机菜单并返回菜单按钮焦点 |
| 页面完善 | 中文 404、单一主内容区域、搜索焦点提示、攻略参考入口、纠错入口、更新记录 |
| 分享与索引 | 绝对 canonical 与分享图、360 个正式页面的 sitemap；站点版本与旧攻略核对日期分开呈现 |
| 验收与交付 | 独立 Playwright 回归目录及跨平台启动/关闭脚本；CI 加入浏览器回归；预览默认指向成品且显示构建信息；成品 Git 属性保留原始字节 |

## 实测变化

同为 375×812 手机视口：

- 百科索引单页高度由 **129,018px → 5,027px**，单次展示从 328 张大卡片变为 24 条紧凑档案。分页遍历取得 328 个唯一条目，无遗漏。
- 波兰水饺配方区起点由 **914px → 646px**，且可通过顶部“配方”按钮直接跳转。
- 逐字输入“怎么复活”后获得 **5 条结果**；原版本为 0。
- 搜索结果进入详情后返回，关键词和已展开的结果批次保留；料理第二页进入详情后返回及刷新，分类和页码保留。

上述长度变化是查阅布局测量，不等同于真实网络性能提升比例。

## 验证证据

| 验证 | 结果 |
| --- | --- |
| `npm.cmd run validate:content` | 45 个 Markdown、328 个结构化条目与图标，0 错误 |
| `npm.cmd test` | 10 个文件、192 项通过 |
| `npm.cmd run typecheck` | 退出码 0 |
| `npm.cmd run generate` | 最终构建完成；985 次预渲染路由任务，包含数据和子路径处理；不是 985 个独立内容页面 |
| `npm.cmd run test:e2e` | 最终构建 18/18 通过，桌面与手机各 9 项；耗时 10.9 秒 |
| 最终成品浏览器检查 | 14 个地址 × 2 种视口，共 28 次；HTTP 200、无破图、无整页横向溢出、无 console error/pageerror/hydration 报错 |
| 静态链接 | sitemap 360 页加搜索页，共 361 个地址，全部 HTTP 200 |
| 百科分页闭环 | 14 页共取得 328 个唯一条目 |
| 旧进度 | 注入旧存储格式后恢复原节点；勾选刷新、取消重置、确认重置均通过 |
| 成品完整性 | 构建清单 1,135 个站点文件逐字节哈希通过；另有清单自身与 `build-info.json` |
| 源码与成品对应 | `app`、`content`、`public` 当前字节聚合哈希与成品 `source-state.json` 相同 |
| `git diff --check` | 最终通过 |

单元测试和类型检查在逻辑最终状态验证后，仅追加两处 CSS 排版修整；这些证据继续适用。最后 CSS 修整后重新生成、运行 18 项浏览器回归、导出并检查实际成品。

Windows 曾短暂拒绝成品目录重命名。导出脚本保留旧成品且未遗留 staging/lock；确认测试预览端口释放后再次导出成功。最后检查的是成功替换后的版本。

## 成品与复现

成品目录：`C:\Users\67588\Documents\饥荒百科全书\成品文件\github-pages`

在项目根目录运行：

```powershell
npm.cmd run preview:static
```

打开 `http://127.0.0.1:4173/jihuang/`。这是带 `/jihuang/` 子路径的静态网站，需要 HTTP 预览，不适合直接双击 HTML。

再次验证：

```powershell
npm.cmd run test:e2e
node '过程文件/验证报告/2026-09-09-release/verify.mjs'
```

浏览器回归默认检查 `.output/public`；成品验证脚本检查 `成品文件/github-pages`，同时核对当前运行源码哈希。

构建清单 SHA-256：`AD12A8E46DA3B50F94F4BFBF0E138D47F86C3A58C087644EBD7C5C8A74024759`。

运行源码 SHA-256：`717052D91F9A3351FCF9A91AD48AD331ADE387A25D4F799A444AAC6B0628D30F`。

`build-info.json` 中的提交是工作区基准 `bb93cba`，本次修改未提交；`source-state.json` 明确记录 `workingTreeModified: true` 与实际运行源码哈希，不能将基准提交误称为本轮发布提交。

## 截图与记录

- [桌面百科索引](2026-09-09-release/screenshots/1440-wiki.png)
- [手机百科索引](2026-09-09-release/screenshots/375-wiki.png)
- [手机料理详情](2026-09-09-release/screenshots/375-wiki_pierogi.png)
- [暗影圣所清单](2026-09-09-release/screenshots/375-tools_progression-checklist_route_shadow-sanctum.png)
- [最终成品检查 JSON](2026-09-09-release/results.json)
- [18 项浏览器回归 JSON](2026-09-09-release/e2e-report.json)

## 本轮边界

角色正文仍为原来的 5 篇，本轮改善目录、参考入口和查阅体验，没有声称完成全角色技能树攻略。Boss 15 项复用已有内容，不是新增 10 篇独立长文。

未制作四格锅模拟器、多世界存档或离线缓存；这些属于后续独立功能。料理食材筛选仅覆盖已收录的示例配方，不能当作完整可替代食材规则引擎。

未重新逐条核对最新游戏更新，旧内容日期保持真实；未运行真实手机或弱网基准测试；未推送或确认线上部署。CI 配置已更新，本轮只验证本地 Windows Chrome 环境。
